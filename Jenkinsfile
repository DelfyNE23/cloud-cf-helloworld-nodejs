pipeline {
    agent any
    stages {
        stage("prepare") {
            steps {
                echo 'Preparing the application'
            }
        }
        stage("build") {
            steps {
                echo 'Building the application'
            }
        }
        stage("test") {
             steps {
                echo 'Testing the application'
            }
        }
    }
}
